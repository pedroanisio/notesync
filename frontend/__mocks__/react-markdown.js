// Mock implementation for react-markdown
import React from 'react';

const ReactMarkdown = ({ children }) => {
  return <div className="markdown-content">{children}</div>;
};

export default ReactMarkdown; 